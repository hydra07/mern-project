const OAuth = () => {
  return (
    <button
      type="button"
      className="bg-red-500 flex items-center px-4 py-2 rounded"
    >
      Sign in with Google
    </button>
  );
};

export default OAuth;
